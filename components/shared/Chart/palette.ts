/**
 * @deprecated Import from "@/lib/chart" instead — the palette moved to the
 * platform-agnostic core (lib/chart/palette.ts) so React Native can share it.
 */
export * from "@/lib/chart/palette";
