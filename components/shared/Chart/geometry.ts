/**
 * @deprecated Import from "@/lib/chart" instead — the geometry moved to the
 * platform-agnostic core (lib/chart/geometry.ts) so React Native can share it.
 */
export * from "@/lib/chart/geometry";
